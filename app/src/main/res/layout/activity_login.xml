<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable name="loginViewModel" type="com.example.mobv.viewModels.LoginViewModel" />
        <variable name="meno" type="String" />
        <variable name="heslo" type="String" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".activities.LoginActivity">

        <include
            android:id="@+id/toolbar"
            layout="@layout/bar_layout" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="675dp"
            android:layout_below="@+id/toolbar"
            android:gravity="center_horizontal"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="268dp"
                android:text="Log into the dark side!"
                android:textSize="22dp"
                android:textStyle="bold" />

            <EditText
                android:id="@+id/email"
                style="@style/Base.Widget.MaterialComponents.TextInputEditText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:hint="meno"
                android:text="@{meno}"
                app:met_floatingLabel="normal" />

            <EditText
                android:id="@+id/password"
                style="@style/Base.Widget.MaterialComponents.TextInputEditText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:hint="heslo"
                android:inputType="textPassword"
                android:text="@{heslo}"
                app:met_floatingLabel="normal" />

            <Button
                android:id="@+id/btn_login"
                android:layout_width="match_parent"
                android:layout_height="198dp"
                android:layout_marginTop="10dp"
                android:background="#ffc845"
                android:onClick="@{() -> loginViewModel.login(email.getText().toString(), password.getText().toString())}"
                android:padding="20dp"
                android:text="Let's GO!"
                android:textColor="#fff"
                android:textSize="30dp" />

        </LinearLayout>

    </RelativeLayout>
</layout>